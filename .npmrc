# Ensure lifecycle scripts run (Electron postinstall downloads the binary).
ignore-scripts=false

# Use a mirror when GitHub releases time out (e.g. RequestError: socket hang up).
# @electron/get reads this as npm_config_electron_mirror during postinstall.
electron_mirror=https://npmmirror.com/mirrors/electron/
